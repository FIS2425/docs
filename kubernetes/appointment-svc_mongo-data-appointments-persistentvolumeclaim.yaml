apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-data-appointments
  name: mongo-data-appointments
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
